$(document).ready(initialize);

var balance = 1000;

function initialize()
{

}

function handle_deposit(balance, amount)
{
  var balance = #balance;
  return balance += parseInt(string);
}

function handle_withdrawal()
{
  return
}